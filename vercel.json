{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.9 --activate && cd app && pnpm install --frozen-lockfile",
  "buildCommand": "cd app && pnpm build",
  "outputDirectory": "app/.next"
}
