---
name: doc-management
description: Управление размером и структурой документации проекта
disable-model-invocation: false
---

# Управление документацией проекта

Этот skill автоматически проверяет и реорганизует документацию согласно лимитам из @.claude/rules/09-documentation-size-limits.md

## Когда применяется

- Когда документ превышает установленные лимиты строк
- При создании новых документов в docs/
- При обновлении BACKLOG или COMPLETED_TASKS

## Workflow

### 1. Проверка размеров

Автоматически проверить все документы:

```bash
find docs/ .claude/ CLAUDE.md -name "*.md" -type f | while read f; do
  lines=$(wc -l < "$f")
  basename=$(basename "$f")

  # Определить лимит
  limit=300
  if [[ "$basename" == "CLAUDE.md" ]]; then
    limit=80
  elif [[ "$f" == *".claude/rules/"* ]]; then
    limit=150
  fi

  if [ "$lines" -gt "$limit" ]; then
    echo "⚠️  ПРЕВЫШЕН ЛИМИТ: $f ($lines/$limit строк)"
  elif [ "$lines" -gt $((limit - 20)) ]; then
    echo "⚡ БЛИЗКО К ЛИМИТУ: $f ($lines/$limit строк)"
  fi
done
```

### 2. Разбиение BACKLOG.md

Если `docs/BACKLOG.md` > 280 строк:

1. Создать структуру:
```bash
mkdir -p docs/backlog
```

2. Создать индекс:
```markdown
# docs/backlog/INDEX.md
# 📋 Бэклог задач

## Критичные задачи
См. @BACKLOG_CRITICAL.md

## Высокий приоритет
См. @BACKLOG_HIGH.md

## Средний приоритет
См. @BACKLOG_MEDIUM.md

## Низкий приоритет
См. @BACKLOG_LOW.md
```

3. Разбить по приоритетам:
- Извлечь секцию "КРИТИЧНЫЕ ЗАДАЧИ" → `BACKLOG_CRITICAL.md`
- Извлечь секцию "ВЫСОКИЙ ПРИОРИТЕТ" → `BACKLOG_HIGH.md`
- Извлечь секцию "СРЕДНИЙ ПРИОРИТЕТ" → `BACKLOG_MEDIUM.md`
- Извлечь секцию "НИЗКИЙ ПРИОРИТЕТ" → `BACKLOG_LOW.md`

4. Заменить оригинальный файл на ссылку:
```markdown
# docs/BACKLOG.md
# 📋 Бэклог задач

⚠️ **ЭТОТ ФАЙЛ БЫЛ РАЗБИТ НА ЧАСТИ**

См. полную структуру: @backlog/INDEX.md
```

### 3. Разбиение COMPLETED_TASKS.md

Если `docs/COMPLETED_TASKS.md` > 280 строк:

1. Создать структуру:
```bash
mkdir -p docs/completed
```

2. Создать индекс:
```markdown
# docs/completed/INDEX.md
# ✅ Выполненные задачи

## 2026
- @COMPLETED_2026-01.md - Январь 2026
- @COMPLETED_2026-02.md - Февраль 2026

## 2025
- @COMPLETED_2025-12.md - Декабрь 2025
```

3. Разбить по месяцам:
- Группировать задачи по датам обновления
- Создать файл для каждого месяца
- Сохранить структуру фаз и секций

### 4. Разбиение API_SPEC.md

Если `docs/API_SPEC.md` > 280 строк:

1. Создать структуру:
```bash
mkdir -p docs/api
```

2. Создать индекс:
```markdown
# docs/api/API_INDEX.md
# 🔌 API Спецификация

**Базовый URL**: `/api/trpc`

## Группы эндпоинтов

- @API_AUTH.md - Аутентификация (NextAuth.js)
- @API_PRODUCTS.md - Товары и каталог
- @API_CART.md - Корзина
- @API_ORDERS.md - Заказы
- @API_REVIEWS.md - Отзывы
- @API_PROMO.md - Промокоды
- @API_ADMIN.md - Административные функции
- @API_WEBHOOKS.md - Webhooks (ЮKassa)
```

3. Разбить по группам:
- Извлечь секции по логическим группам
- Сохранить примеры и форматы ошибок
- Добавить кросс-ссылки между группами

### 5. Разбиение DATABASE_SCHEMA.md

Если `docs/DATABASE_SCHEMA.md` > 280 строк:

1. Создать структуру:
```bash
mkdir -p docs/database
```

2. Создать индекс с ER-диаграммой:
```markdown
# docs/database/DATABASE_INDEX.md
# 🗄️ Схема базы данных

## ER-диаграмма
[Текстовое представление связей между таблицами]

## Группы таблиц

- @DATABASE_USERS.md - Пользователи и адреса
- @DATABASE_CATALOG.md - Категории и товары
- @DATABASE_ORDERS.md - Заказы и история
- @DATABASE_CONTENT.md - Отзывы, страницы, блог
- @DATABASE_PROMO.md - Промокоды и акции
```

3. Разбить по группам:
- Логически связанные таблицы в один файл
- Сохранить индексы и связи
- Добавить примеры запросов в соответствующие файлы

## Обновление CLAUDE.md

После реорганизации обновить импорты в корневом CLAUDE.md:

```markdown
## Для производственной архитектуры

- API: @docs/api/API_INDEX.md
- БД: @docs/database/DATABASE_INDEX.md
- Бэклог: @docs/backlog/INDEX.md
```

## Проверка целостности

После разбиения проверить:

1. ✅ Все ссылки работают
2. ✅ Нет дублирования контента
3. ✅ Каждый файл < лимита
4. ✅ INDEX файлы актуальны
5. ✅ Импорты в CLAUDE.md корректны

## Автоматизация

Добавить pre-commit hook для проверки:

```bash
#!/bin/bash
# .claude/hooks/check-doc-size.sh

exceeded=0
find docs/ .claude/ -name "*.md" -type f | while read f; do
  lines=$(wc -l < "$f")
  if [ "$lines" -gt 300 ]; then
    echo "❌ $f превышает лимит: $lines строк"
    exceeded=1
  fi
done

if [ "$exceeded" -eq 1 ]; then
  echo ""
  echo "Используйте /doc-management для автоматической реорганизации"
  exit 1
fi
```
