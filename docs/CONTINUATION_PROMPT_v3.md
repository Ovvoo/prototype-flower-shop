# 🌸 CONTINUATION PROMPT v3 | Интернет-магазин цветов

**Дата создания**: 28 января 2026, 02:00
**Текущий прогресс**: 72% (Фаза 2: 100% ✅)
**Последняя завершенная задача**: Order History Page (6 файлов, 770 строк)

---

## 📊 ТЕКУЩИЙ СТАТУС ПРОЕКТА

### Общий прогресс

```
┌─────────────────────────────────────────────────────────────┐
│                   ROADMAP ПРОЕКТА                           │
│                                                             │
│  ФАЗА 0: Setup & Infrastructure         ████████████ 100%  │
│  ФАЗА 1: Backend API                    ████████████ 100%  │
│  ФАЗА 2: Frontend - Публичная часть     ████████████ 100%  │
│  ФАЗА 3: Админ-панель                   ░░░░░░░░░░░░   0%  │
│  ФАЗА 4: Доп. страницы & SEO            ░░░░░░░░░░░░   0%  │
│  ФАЗА 5: Интеграции                     ░░░░░░░░░░░░   0%  │
│                                                             │
│  ОБЩИЙ ПРОГРЕСС                         █████████░░░  72%  │
└─────────────────────────────────────────────────────────────┘
```

### Что сделано за последние сессии

**Сессия 1** (27 января):
- ✅ Setup (Laravel 11 + Next.js 16 + PostgreSQL 16)
- ✅ База данных (12 таблиц, seeders)
- ✅ Backend API (30+ endpoints)
- ✅ Frontend (4 страницы: главная, каталог, товар, корзина)

**Сессия 2** (28 января, утро):
- ✅ Checkout flow (18 файлов, 4-шаговая форма)
- ✅ Order success page
- ✅ Auth Modal (5 файлов: вход + регистрация)
- ✅ Profile Page (6 файлов: редактирование профиля + смена пароля)

**Сессия 3** (28 января, ночь):
- ✅ Order History Page (6 файлов, 770 строк)
- ✅ Список заказов с фильтрацией и сортировкой
- ✅ Детальная страница заказа
- ✅ Mock данные для демонстрации

**Итого создано**: 135 файлов, 73 компонента, 11 страниц

---

## 🎯 СЛЕДУЮЩАЯ ЗАДАЧА: Admin Panel (Фаза 3)

### Описание задачи

**EPIC-3** из `/docs/backlog/BACKLOG_CRITICAL.md` - Админ-панель для управления магазином

**Что нужно создать**:

#### Backend (Laravel) - 15 часов
1. **Middleware: IsAdmin** - проверка роли администратора
2. **AdminOrderController** - CRUD заказов
   - GET /api/admin/orders (список с фильтрами)
   - GET /api/admin/orders/{id} (детали)
   - PUT /api/admin/orders/{id}/status (обновить статус)
3. **AdminProductController** - CRUD товаров
   - GET /api/admin/products (список)
   - POST /api/admin/products (создать)
   - PUT /api/admin/products/{id} (обновить)
   - DELETE /api/admin/products/{id} (удалить)
4. **Form Requests** для валидации админских запросов
5. **Routes** в `routes/api.php`

#### Frontend (Next.js) - 28 часов
1. **Admin Layout** (`/app/admin/layout.tsx`)
   - Sidebar с навигацией
   - Dashboard, Orders, Products, Settings
   - Защита маршрута (проверка роли)

2. **Dashboard** (`/app/admin/page.tsx`)
   - Статистика: выручка, заказы, клиенты
   - Графики продаж
   - Последние заказы

3. **Orders Management** (`/app/admin/orders/`)
   - Список заказов с фильтрами
   - Смена статусов заказов
   - Детальный просмотр

4. **Products Management** (`/app/admin/products/`)
   - Список товаров
   - Создание/редактирование товара
   - Загрузка изображений
   - Управление категориями

5. **Компоненты**:
   - `<DataTable>` - таблица с пагинацией
   - `<StatusSelector>` - выбор статуса
   - `<ImageUpload>` - загрузка фото
   - `<AdminCard>` - карточка статистики

**Оценка**: 43 часа
**Приоритет**: 🔴 Критичный (необходим для MVP)
**Файлов создать**: ~20-25 файлов

---

## 📋 ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА КОДИРОВАНИЯ

### Источники правил
- **Главный**: `/root/projects/prototype-flower-shop/CLAUDE.md` (66 строк)
- **Дополнительно**: `/.claude/rules/*.md` (9 файлов с best practices)

### Критически важные правила

#### 1. Размеры файлов (MANDATORY)
```
- Components:  < 200 строк
- Pages:       < 200 строк
- Controllers: < 300 строк
- Validation:  < 150 строк
```

**Если файл превышает лимит** → разбить на несколько модулей!

#### 2. TypeScript строгость
```typescript
// ✅ ПРАВИЛЬНО
interface AdminDashboardStats {
  revenue: number
  ordersCount: number
  customersCount: number
}

export function AdminDashboard(): JSX.Element {
  const [stats, setStats] = useState<AdminDashboardStats | null>(null)
  // ...
}

// ❌ НЕПРАВИЛЬНО
export function AdminDashboard() {  // ❌ нет типа возврата
  const [stats, setStats] = useState(null)  // ❌ нет типа
}
```

#### 3. React 19 + Next.js 16 особенности
```typescript
// ✅ Server Component (default)
export default async function AdminPage() {
  // async data fetching
}

// ✅ Client Component (для useState, onClick)
'use client'
export function DataTable() {
  const [data, setData] = useState([])
}

// ✅ Next.js 16: params теперь async!
export default async function Page({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params  // ОБЯЗАТЕЛЬНО await
}
```

#### 4. Модульность (DRY принцип)
- Если код повторяется 2+ раза → вынести в функцию/компонент
- Каждый компонент = одна ответственность
- Переиспользовать существующие UI компоненты:
  - `<Button>` из `components/ui/Button.tsx`
  - `<Input>` из `components/ui/Input.tsx`
  - `<Modal>` из `components/ui/Modal.tsx`

#### 5. UI/UX стандарты проекта
- **Цвета**: `pink-600` (primary), `hover:bg-pink-700`
- **Карточки**: `rounded-2xl shadow-lg`
- **Кнопки**: `rounded-lg px-6 py-3`
- **Язык UI**: Русский (всегда!)
- **Валюта**: ₽ (рубли)

---

## 📁 СТРУКТУРА ПРОЕКТА

### Frontend (Next.js 16)
```
app/
├── app/                          # Pages (App Router)
│   ├── page.tsx                 # Главная ✅
│   ├── catalog/page.tsx         # Каталог ✅
│   ├── product/[id]/page.tsx    # Товар ✅
│   ├── cart/page.tsx            # Корзина ✅
│   ├── checkout/page.tsx        # Оформление заказа ✅
│   ├── order/
│   │   ├── success/page.tsx     # Успешный заказ ✅
│   │   └── [orderNumber]/page.tsx # Детали заказа ✅
│   ├── profile/
│   │   ├── layout.tsx           # Layout с sidebar ✅
│   │   ├── page.tsx             # Профиль ✅
│   │   └── orders/page.tsx      # История заказов ✅
│   └── admin/                   # ⏳ СЛЕДУЮЩАЯ ЗАДАЧА
│       ├── layout.tsx           # (создать)
│       ├── page.tsx             # Dashboard (создать)
│       ├── orders/              # (создать)
│       │   ├── page.tsx
│       │   └── [id]/page.tsx
│       └── products/            # (создать)
│           ├── page.tsx
│           ├── new/page.tsx
│           └── [id]/edit/page.tsx
│
├── components/
│   ├── ui/                      # UI Kit (7 компонентов) ✅
│   ├── auth/                    # Auth (3 компонента) ✅
│   ├── checkout/                # Checkout (15 компонентов) ✅
│   ├── profile/                 # Profile (3 компонента) ✅
│   ├── orders/                  # Orders (4 компонента) ✅
│   └── admin/                   # ⏳ СОЗДАТЬ
│       ├── AdminSidebar.tsx     # (создать)
│       ├── DataTable.tsx        # (создать)
│       ├── StatusSelector.tsx   # (создать)
│       ├── ImageUpload.tsx      # (создать)
│       └── AdminCard.tsx        # (создать)
│
├── lib/
│   ├── types/                   # TypeScript типы (9 файлов) ✅
│   ├── api/                     # API services (6 файлов) ✅
│   │   └── admin.ts             # ⏳ СОЗДАТЬ
│   ├── hooks/                   # React hooks (5 файлов) ✅
│   │   └── useAdminStats.ts     # ⏳ СОЗДАТЬ
│   ├── validation/              # Yup schemas (3 файла) ✅
│   │   └── adminSchema.ts       # ⏳ СОЗДАТЬ
│   └── mock/
│       ├── orders.ts            # ✅
│       └── adminStats.ts        # ⏳ СОЗДАТЬ
│
└── contexts/
    └── CartContext.tsx          # ✅
```

### Backend (Laravel 11)
```
backend/
├── app/
│   ├── Models/              # 12 Eloquent models ✅
│   ├── Http/
│   │   ├── Controllers/
│   │   │   ├── Api/         # Public API (6 controllers) ✅
│   │   │   └── Admin/       # ⏳ СОЗДАТЬ
│   │   │       ├── OrderController.php
│   │   │       └── ProductController.php
│   │   ├── Middleware/
│   │   │   └── IsAdmin.php  # ⏳ СОЗДАТЬ
│   │   ├── Resources/       # 7 API resources ✅
│   │   └── Requests/        # 5 form requests ✅
│   │       └── Admin/       # ⏳ СОЗДАТЬ
│   │           ├── UpdateOrderStatusRequest.php
│   │           └── StoreProductRequest.php
│   └── Services/
│       └── OrderService.php # ✅
│
└── routes/
    └── api.php              # 30+ endpoints ✅
                             # Добавить /api/admin/* routes
```

---

## 🔧 СУЩЕСТВУЮЩИЕ КОМПОНЕНТЫ И API

### Order Types (готовы к использованию)

```typescript
// lib/types/order.ts
export interface Order {
  id: number
  order_number: string
  status: OrderStatus
  payment_status: PaymentStatus
  total_amount: number
  items: OrderItem[]
  // ... полная информация
}

export type OrderStatus =
  | 'new'           // Новый
  | 'confirmed'     // Подтвержден
  | 'preparing'     // В обработке
  | 'delivering'    // Доставляется
  | 'completed'     // Доставлен
  | 'cancelled'     // Отменен
```

### Orders API (готов)

```typescript
// lib/api/orders.ts
export const ordersApi = {
  create(data: CreateOrderRequest): Promise<{ order: Order }>
  getOrders(params?: { status?: OrderStatus }): Promise<PaginatedResponse<Order>>
  getOrder(orderNumber: string): Promise<{ order: Order }>
}
```

### Существующие UI компоненты

```typescript
// Используй эти компоненты в админке
import { Button } from '@/components/ui/Button'
import { Input } from '@/components/ui/Input'
import { Select } from '@/components/ui/Select'
import { Modal } from '@/components/ui/Modal'
import { OrderStatusBadge } from '@/components/orders/OrderStatusBadge'
```

---

## 🚀 WORKFLOW ДЛЯ НОВОЙ ЗАДАЧИ

### Шаг 1: Планирование

**Задай 1-2 уточняющих вопроса** пользователю:
1. Какие данные показывать на Dashboard?
2. Какие фильтры нужны для списка заказов?
3. Какие поля должны быть в форме товара?

### Шаг 2: Backend (Laravel)

1. **Создай Middleware** (`app/Http/Middleware/IsAdmin.php`)
2. **Создай Controllers** в `app/Http/Controllers/Admin/`
3. **Создай Form Requests** для валидации
4. **Обнови routes** в `routes/api.php`
5. **Протестируй API** через Postman или curl

### Шаг 3: Frontend (Next.js)

1. **Создай типы** для админки (`lib/types/admin.ts`)
2. **Создай API service** (`lib/api/admin.ts`)
3. **Создай хуки** (`lib/hooks/useAdminStats.ts`)
4. **Создай компоненты** по порядку:
   - AdminSidebar (< 100 строк)
   - AdminCard (< 80 строк)
   - DataTable (< 200 строк)
   - StatusSelector (< 100 строк)
5. **Создай страницы**:
   - `app/admin/layout.tsx` (< 100 строк)
   - `app/admin/page.tsx` (< 200 строк)
   - `app/admin/orders/page.tsx` (< 200 строк)
   - `app/admin/products/page.tsx` (< 200 строк)

### Шаг 4: Проверка

```bash
# Backend
cd /root/projects/prototype-flower-shop/backend
php artisan test

# Frontend
cd /root/projects/prototype-flower-shop/app
npx tsc --noEmit
pnpm build
pnpm dev
```

### Шаг 5: Обнови документацию

1. **Добавь секцию** в `/docs/COMPLETED_TASKS.md`
2. **Обнови прогресс** в `/docs/PROGRESS.md`:
   - Фаза 3: 0% → X%
   - Общий прогресс: 72% → Y%

---

## ⚠️ ВАЖНЫЕ ОГРАНИЧЕНИЯ

### Что МОЖНО делать сейчас
✅ Backend Controllers и API endpoints
✅ Frontend компоненты и страницы
✅ Формы с валидацией
✅ Client-side состояние
✅ Mock данные для демонстрации

### Что НЕЛЬЗЯ делать без интеграции
❌ Реальная загрузка изображений (нужен storage)
❌ Email уведомления (нужен mail server)
❌ Real-time updates (нужен WebSocket)

**Решение**: Используй mock данные и заглушки, оставь TODO комментарии для будущей интеграции.

---

## 📚 ПОЛЕЗНЫЕ ФАЙЛЫ ДЛЯ REFERENCE

### Backend примеры
- `backend/app/Http/Controllers/Api/OrderController.php` - пример контроллера
- `backend/app/Http/Requests/CreateOrderRequest.php` - пример валидации
- `backend/routes/api.php` - текущие routes

### Frontend примеры
- `app/app/profile/layout.tsx` - пример layout с sidebar
- `app/components/orders/OrdersFilter.tsx` - пример фильтра
- `app/components/orders/OrderCard.tsx` - пример карточки
- `app/app/profile/orders/page.tsx` - пример списка с фильтрами

---

## 💡 СОВЕТЫ ДЛЯ ТЕБЯ (Claude)

### Как начать работу в новом чате

1. **Прочитай ключевые файлы**:
   ```
   /docs/backlog/BACKLOG_CRITICAL.md (строки 200-350) - детали Admin Panel
   /backend/app/Http/Controllers/Api/OrderController.php - пример контроллера
   /app/app/profile/orders/page.tsx - пример admin-like страницы
   ```

2. **Задай 1-2 уточняющих вопроса** пользователю (интерактивно, не списком)

3. **Начни с Backend**:
   - Сначала Middleware (самый простой)
   - Потом AdminOrderController
   - Затем AdminProductController

4. **Потом Frontend**:
   - Сначала типы и API service
   - Потом AdminSidebar и Layout
   - Затем Dashboard
   - Последними Orders и Products страницы

5. **Проверь размеры файлов** перед коммитом

6. **Обнови документацию** после завершения

### Частые ошибки (НЕ делай так)

❌ Создавать файлы > 200 строк (разбивай на модули!)
❌ Использовать `any` в TypeScript
❌ Забывать `'use client'` для компонентов с useState/onClick
❌ Забывать `await params` в Next.js 16
❌ Дублировать код (используй DRY!)
❌ Забывать обновлять документацию

### Что делать если застрял

1. Прочитай `CLAUDE.md` - там все правила
2. Посмотри на похожие компоненты (OrderCard, ProfileSidebar)
3. Используй существующие UI компоненты (Button, Modal, Input)
4. Проверь типы в `lib/types/`

---

## 📞 КАК ПОСМОТРЕТЬ РЕЗУЛЬТАТ

### Dev сервер уже запущен ✅

Открой в браузере:

**Frontend:**
- Главная: `http://localhost:3000`
- Каталог: `http://localhost:3000/catalog`
- Корзина: `http://localhost:3000/cart`
- Профиль: `http://localhost:3000/profile`
- **История заказов (НОВОЕ!)**: `http://localhost:3000/profile/orders`
- **Детали заказа**: `http://localhost:3000/order/000125`

**Backend API (через curl или Postman):**
```bash
# Список товаров
curl http://localhost:8000/api/products

# Список заказов (требует auth)
curl -H "Authorization: Bearer {token}" http://localhost:8000/api/orders
```

---

## ✅ ЧЕКЛИСТ ПЕРЕД НАЧАЛОМ РАБОТЫ

- [ ] Dev сервер запущен (`pnpm dev`)
- [ ] Прочитал `/docs/backlog/BACKLOG_CRITICAL.md` (Admin Panel секция)
- [ ] Понял структуру существующих контроллеров
- [ ] Задал уточняющие вопросы пользователю
- [ ] Определил порядок создания файлов (Backend → Frontend)
- [ ] Готов создавать компоненты < 200 строк

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После выполнения Admin Panel (Фаза 3):

**Backend:**
- ✅ 2 новых контроллера (AdminOrderController, AdminProductController)
- ✅ Middleware IsAdmin
- ✅ ~10 новых API endpoints
- ✅ 3-5 Form Requests для валидации

**Frontend:**
- ✅ Admin Layout с sidebar навигацией
- ✅ Dashboard с статистикой
- ✅ Страница управления заказами
- ✅ Страница управления товарами
- ✅ ~10 новых компонентов

**Прогресс:**
- Фаза 3: 0% → 100% ✅
- Общий прогресс: 72% → ~85%

---

## 🚀 ПОСЛЕ ЗАВЕРШЕНИЯ ADMIN PANEL

**Следующие шаги** (спроси пользователя):
1. **Фаза 4**: Дополнительные страницы (О компании, Контакты, Блог)
2. **Интеграции**: Email уведомления, ЮKassa
3. **Улучшения**: Performance оптимизация, SEO

---

**Готов начинать?** 🚀

Прочитай эти файлы для начала:
1. `/docs/backlog/BACKLOG_CRITICAL.md` (строки 200-350) - детали Admin Panel
2. `/backend/app/Http/Controllers/Api/OrderController.php` - пример контроллера
3. `/app/app/profile/layout.tsx` - пример layout

Затем **задай мне 1-2 вопроса** по дизайну админки, и начинаем кодить!

---

**Версия**: 3.0
**Дата**: 28 января 2026, 02:00
**Автор**: Claude Sonnet 4.5
**Последнее обновление**: После завершения Order History Page
**Прогресс проекта**: 72% (Фаза 2 завершена на 100%)
