# 🚀 QUICK START - Копируй в новый чат

```
Привет! Продолжаю разработку интернет-магазина цветов.

📊 Текущий прогресс: 69% (Фаза 2: 95%)
📁 Последняя задача: Profile Page ✅ (6 файлов, 517 строк)

🎯 Следующая задача: Order History Page (TASK-2.6)
   - Страница /profile/orders с историей заказов
   - Карточки заказов с статусами
   - Фильтрация, детали заказов
   - Mock данные (без API)
   - Оценка: 6 часов
   - Последняя задача Фазы 2!

⚠️ КРИТИЧЕСКИ ВАЖНО:
1. Прочитай /docs/CONTINUATION_PROMPT_v2.md для ПОЛНОГО контекста
2. Прочитай /.clinerules для правил кодирования
3. Соблюдай лимиты размеров файлов (< 200 строк)
4. Используй TypeScript strict mode (нет any!)
5. React Hook Form + Yup для форм

📋 Начни работу так:
1. Прочитай /docs/BACKLOG.md (строки 360-395) - детали задачи
2. Прочитай /lib/types/order.ts - типы Order, OrderStatus
3. Посмотри /components/profile/ProfileSidebar.tsx - sidebar готов
4. ЗАДАЙ МНЕ 1-2 ВОПРОСА по дизайну (интерактивно, не списком!)
5. Создавай компоненты от простого к сложному

🔧 Команды:
cd /root/projects/prototype-flower-shop/app
pnpm dev              # Запуск dev server
npx tsc --noEmit      # Проверка TypeScript

После завершения обнови:
- /docs/COMPLETED_TASKS.md (добавь секцию Order History)
- /docs/PROGRESS.md (Фаза 2: 95% → 100%, Общий: 69% → 72%)

Начинаем! Задавай вопросы и приступаем к Order History Page.
```

---

## 💡 Дополнительная информация (если нужна)

**Стек**: Next.js 16 + React 19 + TypeScript 5.9 + Tailwind CSS v4
**Язык UI**: Русский, валюта: ₽
**Цвета**: pink-600 (primary), rounded-2xl (карточки)

**Созданные компоненты** (можно использовать):
- Button, Input, Select, Textarea, Modal, Skeleton (в /components/ui/)
- ProfileSidebar (в /components/profile/)
- AuthModal, LoginForm, RegisterForm (в /components/auth/)

**Готовые типы**:
- Order, OrderStatus, OrderItem (в /lib/types/order.ts)
- User, UpdateProfileRequest (в /lib/types/user.ts)

**Готовые API**:
- ordersApi.getOrders() - получить список (в /lib/api/orders.ts)
- authApi.getMe() - получить пользователя (в /lib/api/auth.ts)
