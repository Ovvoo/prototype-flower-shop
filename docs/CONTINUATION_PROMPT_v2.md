# 🌸 CONTINUATION PROMPT v2 | Интернет-магазин цветов

**Дата создания**: 28 января 2026
**Текущий прогресс**: 69% (Фаза 2: 95%)
**Последняя завершенная задача**: Profile Page (6 файлов, 517 строк)

---

## 📊 ТЕКУЩИЙ СТАТУС ПРОЕКТА

### Общий прогресс

```
┌─────────────────────────────────────────────────────────────┐
│                   ROADMAP ПРОЕКТА                           │
│                                                             │
│  ФАЗА 0: Setup & Infrastructure         ████████████ 100%  │
│  ФАЗА 1: Backend API                    ████████████ 100%  │
│  ФАЗА 2: Frontend - Публичная часть     ███████████░  95%  │
│  ФАЗА 3: Админ-панель                   ░░░░░░░░░░░░   0%  │
│  ФАЗА 4: Доп. страницы & SEO            ░░░░░░░░░░░░   0%  │
│  ФАЗА 5: Интеграции                     ░░░░░░░░░░░░   0%  │
│                                                             │
│  ОБЩИЙ ПРОГРЕСС                         ████████░░░░  69%  │
└─────────────────────────────────────────────────────────────┘
```

### Что сделано за последние сессии

**Сессия 1** (27 января):
- ✅ Setup (Laravel 11 + Next.js 16 + PostgreSQL 16)
- ✅ База данных (12 таблиц, seeders)
- ✅ Backend API (30+ endpoints)
- ✅ Frontend (4 страницы: главная, каталог, товар, корзина)

**Сессия 2** (28 января):
- ✅ Checkout flow (18 файлов, 4-шаговая форма)
- ✅ Order success page
- ✅ Auth Modal (5 файлов: вход + регистрация)
- ✅ Profile Page (6 файлов: редактирование профиля + смена пароля)

**Итого создано**: 129 файлов, ~67 компонентов, 7 страниц

---

## 🎯 СЛЕДУЮЩАЯ ЗАДАЧА: Order History Page

### Описание задачи

**TASK-2.6** из `/docs/BACKLOG.md` - Последняя задача Фазы 2

**Что нужно создать**:
- Страница `/profile/orders` с историей заказов пользователя
- Список заказов в виде карточек
- Фильтрация по статусу
- Статусные бейджи с цветами
- Детали заказа (раскрывающиеся или отдельная страница)
- Mock данные (без API интеграции)

**Оценка**: 6 часов (из BACKLOG.md)
**Приоритет**: 🔴 Критичный (последняя задача MVP frontend)
**Файлов создать**: ~4-6 файлов, ~600 строк кода

---

## 📋 ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА КОДИРОВАНИЯ

### Источники правил
- **Главный**: `/root/projects/prototype-flower-shop/.clinerules` (772 строки)
- **Проект**: `/root/projects/prototype-flower-shop/CLAUDE.md` (81 строка)
- **Дополнительно**: `/.claude/rules/*.md` (6 файлов с best practices)

### Критически важные правила

#### 1. Размеры файлов (MANDATORY)
```
- Components:  < 200 строк
- Pages:       < 200 строк
- Validation:  < 150 строк
- Services:    < 300 строк
- Utilities:   < 50 строк на функцию
```

**Если файл превышает лимит** → разбить на несколько модулей!

#### 2. TypeScript строгость
```typescript
// ✅ ПРАВИЛЬНО
interface OrderCardProps {
  order: Order
  onRepeat: (orderId: string) => void
}

export function OrderCard({ order, onRepeat }: OrderCardProps): JSX.Element {
  // ...
}

// ❌ НЕПРАВИЛЬНО
export function OrderCard(props: any) {  // ❌ any запрещен
  // ...
}
```

#### 3. React 19 + Next.js 16 особенности
```typescript
// ✅ Server Component (default)
export default async function OrdersPage() {
  // async data fetching
}

// ✅ Client Component (для useState, onClick)
'use client'
export function OrderCard() {
  const [expanded, setExpanded] = useState(false)
}

// ✅ Next.js 16: params теперь async!
export default async function Page({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params  // ОБЯЗАТЕЛЬНО await
}
```

#### 4. Модульность (DRY принцип)
- Если код повторяется 2+ раза → вынести в функцию/компонент
- Каждый компонент = одна ответственность
- Переиспользовать существующие UI компоненты:
  - `<Button>` из `components/ui/Button.tsx`
  - `<Input>` из `components/ui/Input.tsx`
  - `<Modal>` из `components/ui/Modal.tsx`

#### 5. Валидация форм
- **Всегда**: React Hook Form + Yup
- **Типы**: через `InferType<typeof schema>`
- **Пример**:
```typescript
import { yupResolver } from '@hookform/resolvers/yup'
import * as yup from 'yup'

const schema = yup.object({ name: yup.string().required() })
type FormValues = yup.InferType<typeof schema>

const { register, handleSubmit } = useForm<FormValues>({
  resolver: yupResolver(schema)
})
```

#### 6. UI/UX стандарты проекта
- **Цвета**: `pink-600` (primary), `hover:bg-pink-700`
- **Карточки**: `rounded-2xl shadow-lg`
- **Кнопки**: `rounded-lg px-6 py-3`
- **Язык UI**: Русский (всегда!)
- **Валюта**: ₽ (рубли)

---

## 📁 СТРУКТУРА ПРОЕКТА

### Frontend (Next.js 16)
```
app/
├── app/                          # Pages (App Router)
│   ├── page.tsx                 # Главная ✅
│   ├── catalog/page.tsx         # Каталог ✅
│   ├── product/[id]/page.tsx    # Товар ✅
│   ├── cart/page.tsx            # Корзина ✅
│   ├── checkout/page.tsx        # Оформление заказа ✅
│   ├── order/success/page.tsx   # Успешный заказ ✅
│   └── profile/
│       ├── layout.tsx           # Layout с sidebar ✅
│       ├── page.tsx             # Профиль ✅
│       └── orders/              # ⏳ СЛЕДУЮЩАЯ ЗАДАЧА
│           └── page.tsx         # История заказов (создать)
│
├── components/
│   ├── ui/                      # UI Kit (6 компонентов)
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   ├── Select.tsx
│   │   ├── Textarea.tsx
│   │   ├── Skeleton.tsx
│   │   └── Modal.tsx
│   ├── auth/                    # Auth (3 компонента)
│   ├── checkout/                # Checkout (15 компонентов)
│   ├── profile/                 # Profile (3 компонента)
│   └── orders/                  # ⏳ СОЗДАТЬ
│       ├── OrderCard.tsx        # (создать)
│       ├── OrderStatusBadge.tsx # (создать)
│       └── OrderDetails.tsx     # (создать)
│
├── lib/
│   ├── types/                   # TypeScript типы (9 файлов)
│   │   ├── order.ts            # Order, OrderStatus, OrderItem
│   │   ├── user.ts
│   │   └── ...
│   ├── api/                     # API services (6 файлов)
│   │   ├── orders.ts           # ordersApi (готов)
│   │   └── ...
│   ├── hooks/                   # React hooks (4 файла)
│   │   ├── useAuth.ts
│   │   └── ...
│   └── validation/              # Yup schemas (3 файла)
│       ├── authSchema.ts
│       ├── profileSchema.ts
│       └── checkoutSchema.ts
│
└── contexts/
    └── CartContext.tsx          # Глобальное состояние корзины
```

### Backend (Laravel 11)
```
backend/
├── app/
│   ├── Models/              # 12 Eloquent models ✅
│   ├── Http/
│   │   ├── Controllers/     # 6 controllers ✅
│   │   ├── Resources/       # 7 API resources ✅
│   │   └── Requests/        # 5 form requests ✅
│   └── Services/
│       └── OrderService.php # Бизнес-логика заказов ✅
│
├── database/
│   ├── migrations/          # 12 таблиц ✅
│   └── seeders/             # 7 seeders с mock данными ✅
│
└── routes/
    └── api.php              # 30+ endpoints ✅
```

---

## 🔧 СУЩЕСТВУЮЩИЕ ТИПЫ И API

### Order Types (готовы к использованию)

```typescript
// lib/types/order.ts
export interface Order {
  id: number
  order_number: string        // "#000001"
  user_id: number | null

  // Суммы
  subtotal: number
  discount: number
  delivery_fee: number
  total_amount: number

  // Статусы
  status: OrderStatus
  payment_status: PaymentStatus

  // Контакт
  customer_name: string
  customer_email: string
  customer_phone: string

  // Доставка
  delivery_address: DeliveryAddress
  delivery_date: string
  delivery_time: string

  // Товары
  items: OrderItem[]

  created_at: string
}

export type OrderStatus =
  | 'new'           // Новый
  | 'confirmed'     // Подтвержден
  | 'processing'    // В обработке
  | 'shipped'       // Отправлен
  | 'delivered'     // Доставлен
  | 'cancelled'     // Отменен

export type PaymentStatus =
  | 'pending'       // Ожидает оплаты
  | 'paid'          // Оплачен
  | 'failed'        // Ошибка оплаты
  | 'refunded'      // Возврат

export interface OrderItem {
  id: number
  order_id: number
  product_id: number
  product_name: string
  product_image: string | null
  price: number
  quantity: number
  subtotal: number
}
```

### Orders API (готов к использованию)

```typescript
// lib/api/orders.ts
export const ordersApi = {
  /**
   * Создать заказ
   */
  create(data: CreateOrderRequest): Promise<{ order: Order }>

  /**
   * Получить список заказов пользователя
   */
  getOrders(params?: {
    status?: OrderStatus
    page?: number
    limit?: number
  }): Promise<PaginatedResponse<Order>>

  /**
   * Получить заказ по номеру
   */
  getOrder(orderNumber: string): Promise<{ order: Order }>
}
```

---

## 🎨 ПРИМЕРЫ СУЩЕСТВУЮЩИХ КОМПОНЕНТОВ

### Как используются UI компоненты

```typescript
// Button
import { Button } from '@/components/ui/Button'

<Button variant="primary" size="md" onClick={handleClick}>
  Повторить заказ
</Button>

// Modal
import { Modal } from '@/components/ui/Modal'

<Modal isOpen={isOpen} onClose={onClose} title="Детали заказа" size="lg">
  {/* content */}
</Modal>

// Skeleton (loading state)
import { Skeleton } from '@/components/ui/Skeleton'

<Skeleton variant="card" />
```

### Паттерн карточек (уже используется)

```typescript
// components/ProductCard.tsx (пример)
interface ProductCardProps {
  product: Product
  onAddToCart: (id: string) => void
}

export function ProductCard({ product, onAddToCart }: ProductCardProps) {
  return (
    <div className="bg-white rounded-2xl shadow-lg hover:shadow-xl transition-shadow p-6">
      {/* content */}
    </div>
  )
}
```

---

## 🚀 WORKFLOW ДЛЯ НОВОЙ ЗАДАЧИ

### Шаг 1: Планирование (интерактивно)

**Задай мне уточняющие вопросы**, если нужно:
1. Как должны выглядеть карточки заказов?
2. Какие фильтры нужны?
3. Какие действия с заказами?
4. Сколько mock заказов создать?
5. Куда переходить при клике "Подробнее"?

**Не спрашивай всё списком** - задавай по одному вопросу, дай мне ответить, потом следующий.

### Шаг 2: Создание файлов

1. **Создай компоненты** (по порядку):
   - `components/orders/OrderStatusBadge.tsx` (< 50 строк)
   - `components/orders/OrderCard.tsx` (< 200 строк)
   - `components/orders/OrderDetails.tsx` (< 150 строк, если нужен)
   - `components/orders/OrdersFilter.tsx` (< 100 строк, если нужен)

2. **Создай страницу**:
   - `app/profile/orders/page.tsx` (< 200 строк)

3. **Создай mock данные** (если нужны):
   - `lib/mock/orders.ts` (< 150 строк)

### Шаг 3: Проверка

```bash
# Проверь TypeScript
npx tsc --noEmit

# Запусти dev server
pnpm dev

# Открой в браузере
http://localhost:3000/profile/orders
```

### Шаг 4: Обнови документацию

1. **Добавь секцию** в `/docs/COMPLETED_TASKS.md`:
   - Дата выполнения
   - Список созданных файлов
   - Функционал
   - Код соответствует правилам (размеры файлов)

2. **Обнови прогресс** в `/docs/PROGRESS.md`:
   - Фаза 2: 95% → 100% ✅
   - Общий прогресс: 69% → 72%
   - Обнови метрики (файлов, компонентов, страниц)

---

## ⚠️ ВАЖНЫЕ ОГРАНИЧЕНИЯ

### Что МОЖНО делать сейчас
✅ UI компоненты и страницы
✅ Формы с валидацией (react-hook-form + Yup)
✅ Routing (Next.js App Router)
✅ Client-side состояние (useState, Context API)
✅ Mock данные для демонстрации
✅ TypeScript типизация

### Что НЕЛЬЗЯ делать без Backend
❌ Реальные API запросы (backend не запущен)
❌ Авторизация через Laravel Sanctum
❌ Email уведомления
❌ Загрузка изображений на сервер

**Решение**: Используй mock данные для Order History:
```typescript
const mockOrders: Order[] = [
  {
    id: 1,
    order_number: '#000001',
    status: 'delivered',
    payment_status: 'paid',
    total_amount: 5500,
    delivery_date: '2026-01-25',
    // ...
  },
  // ... еще 5-7 заказов
]
```

---

## 📚 ПОЛЕЗНЫЕ ФАЙЛЫ ДЛЯ REFERENCE

### Похожие компоненты (можно подсмотреть паттерны)
- `components/checkout/OrderSummary.tsx` - как отображать товары в заказе
- `components/ProductCard.tsx` - паттерн карточек
- `app/cart/page.tsx` - как работать со списками

### Существующие хуки
- `lib/hooks/useAuth.ts` - для проверки авторизации
- `lib/hooks/useProducts.ts` - пример data fetching hook

### Типы
- `lib/types/order.ts` - Order, OrderStatus, OrderItem
- `lib/types/common.ts` - PaginatedResponse, ApiError

---

## 🎯 ПОСЛЕ ЗАВЕРШЕНИЯ Order History

### Фаза 2 будет завершена на 100%!

**Следующие шаги** (спроси меня):
1. **Начать Фазу 3**: Админ-панель (критично для MVP)
2. **Улучшения Фазы 2**: Страница адресов `/profile/addresses`
3. **Фаза 4**: Дополнительные страницы (О компании, Контакты)

---

## 💡 СОВЕТЫ ДЛЯ ТЕБЯ (Claude)

### Как начать работу в новом чате

1. **Прочитай ключевые файлы**:
   ```
   /docs/BACKLOG.md (строки 360-395) - детали задачи
   /lib/types/order.ts - типы заказов
   /components/ui/Button.tsx - пример UI компонента
   ```

2. **Задай 1-2 уточняющих вопроса** пользователю (НЕ списком, интерактивно)

3. **Создай компоненты** по порядку (от простого к сложному):
   - Сначала OrderStatusBadge (самый простой)
   - Потом OrderCard
   - Затем страницу

4. **Проверь размеры файлов** перед коммитом

5. **Обнови документацию** после завершения

### Частые ошибки (НЕ делай так)

❌ Создавать файлы > 200 строк (разбивай на модули!)
❌ Использовать `any` в TypeScript
❌ Забывать `'use client'` для компонентов с useState/onClick
❌ Забывать `await params` в Next.js 16
❌ Дублировать код (используй DRY!)
❌ Забывать обновлять документацию

### Что делать если застрял

1. Прочитай `.clinerules` - там все правила
2. Посмотри на похожие компоненты (ProductCard, CheckoutForm)
3. Используй существующие UI компоненты (Button, Modal)
4. Проверь типы в `lib/types/order.ts`

---

## 📞 КОМАНДЫ ДЛЯ ЗАПУСКА

```bash
# Перейти в директорию
cd /root/projects/prototype-flower-shop/app

# Запустить dev сервер
pnpm dev

# Проверить TypeScript
npx tsc --noEmit

# Открыть в браузере
http://localhost:3000/profile/orders
```

---

## ✅ ЧЕКЛИСТ ПЕРЕД ЗАВЕРШЕНИЕМ

Перед тем как закончить работу, убедись:

- [ ] Все файлы < 200 строк (проверь `wc -l`)
- [ ] TypeScript компилируется без ошибок
- [ ] Dev server запускается
- [ ] Страница `/profile/orders` открывается
- [ ] Карточки заказов отображаются
- [ ] Фильтры работают (если есть)
- [ ] Кнопки кликабельны
- [ ] Loading state есть
- [ ] Empty state есть (нет заказов)
- [ ] Документация обновлена (COMPLETED_TASKS.md + PROGRESS.md)
- [ ] Код соответствует .clinerules

---

**Готов начинать?** 🚀

Прочитай эти файлы для начала:
1. `/docs/BACKLOG.md` (строки 360-395) - детали задачи
2. `/lib/types/order.ts` - типы заказов
3. `/components/profile/ProfileSidebar.tsx` - sidebar уже создан

Затем **задай мне 1-2 вопроса** по дизайну Order History, и начинаем кодить!

---

**Версия**: 2.0
**Дата**: 28 января 2026
**Автор**: Claude Sonnet 4.5
**Последнее обновление**: После завершения Profile Page
