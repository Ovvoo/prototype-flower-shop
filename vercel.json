{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && cd app && pnpm install --frozen-lockfile",
  "buildCommand": "cd app && pnpm build",
  "outputDirectory": "app/.next"
}
